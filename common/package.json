{
  "name": "@nnticketing/common",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index.js",
  "type": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del-cli ./build/*",
    "build": "npm run clean && tsc",
    "publish": "git add . && git commit -m \"Update\" && npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^6.0.0",
    "typescript": "^5.7.2"
  }
}
